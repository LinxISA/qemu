此文档为VMMA测例编译说明

1. 编译器环境：/home/huanghaofu/V150B100/linx64-linux-gnu
该编译器从 https://cmc-szv.clouddragon.huawei.com/cmcversion/index/releaseView?deltaId=8238074753450498&isSelect=Software&url_data=Software 下载

2. 指令集版本：LinxISA V150B100 V0.13 

3. 编译脚本: make.py, 直接./make.py 即可生成elf文件

4. 目前只能手动测试单个elf，运行elf文件时，屏幕不会有输出，程序需要手动结束(ctrl + a + x)，因为程序执行完测试代码后，
会进入loop循环。后续会修改测例，使其能够在执行完后能退出程序。

5. 因为编译器识别不到printf，无法通过打印输出来直观判断测例的执行情况，所以暂时只能通过log日志来查看结果，
如果日志中出现以下几条指令则测试通过，0x53和0x50分别表示P和S，即pass，如出现0x46和0x4c则表示FL，即fail。
const           83                              # 0x53
set             a1, t#1
const           80                              # 0x50
后续如果编译器支持printf打印，则修改测例代码。

6. 对于异常情况，异常的处理方式是获取寄存器earg0和ecause的值，然后进入loop循环。

7.测例设计思路
测例主要分为4大部分：
1)页表的填充
2)异常向量表的设置
3)设置特权级开启地址翻译
4)通过读写数据，对地址翻译功能进行测试

1)页表的填充

虚拟地址格式（以3层LPT为例）：
 63         39 38          30 29          21 20          12 11             0
|-----EXT-----|-----VAS0-----|-----VAS1-----|-----VAS2-----|-----offset-----|
假设：
VAS0=0x01,VAS1=0x01,VAS2=0x01,offset=0x001

页表用数组保存，每个数组的元素为一个页表项。
page_table_0[0x200] addr:0x80008000
page_table_1[0x200] addr:0x80009000
page_table_2[0x200] addr:0x8000a000
page_table_data[0x200] = {    
    0x7766554433221100, 0xffeeddccbbaa9988,
    0xfedcba9876543210, 0x0123456789abcdef,
    0xfedcba9876543210, 0x0123456789abcdef } 
addr:0x8000b000

每个页表项的格式为：
|下一级页框号|属性权限|

首先，通过寄存器mmtbase的TN0PB来获取第一级页表的页框号，这里将page_table_0的地址当做TN0PB。
然后根据VAS0,VAS1和VAS2，填充页表项,并设置好每个页表项的属性及权限。
page_table_0[VAS0]=0x80009000(page_table_1)
page_table_1[VAS1]=0x8000a000(page_table_2)
page_table_2[VAS2]=0x8000b000(page_table_data)

其中，page_table_data的地址为最终物理地址的基址，offset为0x001，物理地址为0x8000b001
如果是小端访问，则0x8000b001保存的值为0x11,否则为0x77

2)异常向量表的设置
在设置向量表之前，需要填充页表，使开启地址翻译后的指令PC值的VA==PA，即如果翻译前指令PC为0x80001020,
那么翻译后的地址也应该为0x80001020。然后，填充异常向量表地址的页表，并将异常向量表的虚拟地址
写到寄存器EVBASE，如果期间产生异常，程序会跳到EVBASE所保存的地址执行异常例程。
最后设置页表模式和VA切片的大小，并将值写到MMCONFIG寄存器。

虚拟地址长度模式	VA位数	MMCONFIG.Q	MMCONFIG.NL	基数树深度	虚拟地址空间大小
VA36                36      1           0           3           64G
VA39                39      0           0           3           512G
VA44                44      1           1           4           16T
VA48                48      0           1           4           256T
VA52                52      1           2           5           4P
VA57                57      0           2           5           128P

3)设置特权级开启地址翻译
设置特权级为ACR1，因为如果特权级为ACR0，则不会进行地址翻译，VA==PA。
将测试程序的地址设置到ELINK寄存器，然后打开地址翻译模式和设置TN0PB的值，并写入MMTBASE寄存器。
最后执行eret指令，因为eret指令会将特权级从ACR0降到ACR1执行，并且会跳转到ELINK寄存器所保存的
地址去执行下一个指令块，刚好可以执行测试程序。

4)通过读写数据，对地址翻译功能进行测试
前面第一步填充了页表，所以可以通过最终的物理地址，对该地址进行访存操作，比如可以先读0x8000b001地
址的值，该地址的值是初始化数组的时候写死的，然后往该物理地址所对应的虚拟地址写入某个值，再从该虚拟地址
读值。如果这几次读写的值都符合预期，则表明地址翻译成功。
以下为例子：
VA：0x001 | (0x01 << 12) | (0x01 << 21) | (0x01 << 30)
PA: 0x8000b001

read: addr:VA value:0x11
write: addr:VA value:0x42
read: addr:VA value:0x42

异常测例编写：
只需要在填充页表时，对某一级页表项的属性权限进行改写即可，比如：
如果要产生fetch类异常，只需要将页表项的可执行权限置0
如果要产生store类异常，只需要将页表项的可写权限置0
如果要产生load类异常，只需要将页表项的可读权限置0