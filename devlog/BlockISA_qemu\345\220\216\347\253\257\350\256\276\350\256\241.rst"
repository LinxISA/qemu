Linx BlockISA qemu后端设计分析
********************************

*版本*
 * v0.1 2023.4.17  WangZhou init
 * v0.2 2023.4.18  WangZhou 增加指令生成时插入指令的解决办法
 * v0.3 2023.4.25  WangZhou 增加使用lgpr映射虚拟寄存器的办法和head生成的具体办法，
   现在看不用改qemu的主流程。我们保留分析的过程，新加章节描述新的设计，并更新了
   相关的结论。
 * v0.4 2023.5.04  WangZhou 分析call以及跳转中间码的具体实现方法。得到需要把call
   作为BB划分的推论。这里需要改qemu主流程。set_label需要修改qemu主流程。

*简介*
 本文是Linx BlockISA qemu后端实现的一个分析，我们在模拟BlockISA时，实现的是BlockISA
 到qemu IR的翻译，qemu后端是完成IR到BlockISA的翻译。分析依据v0.20的LARM。

 设计过程也是对qemu后端实现的学习过程，相关分析笔记在这里: 
 https://github.com/wangzhou/notes/blob/master/qemu_tcg%E4%B8%AD%E9%97%B4%E7%A0%81%E4%BC%98%E5%8C%96%E5%92%8C%E5%90%8E%E7%AB%AF%E7%BF%BB%E8%AF%91
 如上的分析代码细节比较多，下面是qemu后端翻译的一个基本建模：
 https://github.com/wangzhou/notes/blob/02d4f200d6626e4d4942722f78881f7d9b19d077/qemu_tcg%E5%90%8E%E7%AB%AF%E7%BF%BB%E8%AF%91%E5%9F%BA%E6%9C%AC%E6%A8%A1%E5%9E%8B

总体认知
=========

这个分析的出发点是看看如何做BlockISA的qemu后端翻译。qemu后端翻译可以作为一个ISA
JIT支持的一个代表(比如JVM以及eBPF)，我们可以尝试支持下BlockISA的qemu后端翻译，看
看其中有没有什么坑或者什么创新点。不过，qemu中IR翻译到BlockISA和编译器同事搞编译
器后端是一样的原理，不过qemu上还要遇到qemu本身实现上的约束，解决针对qemu具体实现
中的工程问题。

BlockISA qemu后端要完成的任务可以大致划分为：TB头尾的构造、基本的中间码的翻译、
block划分、寄存器分配等。

qemu后端翻译出的一个TB大致由TB头(prologue)、TB尾(epilogue)和TB业务代码组成，其中
每个TB的头尾都使用固定生成的指令，动态翻译只针对TB业务代码。每个TB是host上的一个
函数，TB头尾主要负责构造函数执行的上下文，头尾可以分别各使用一个block实现，TB业务
代码使用一组block实现(下面会分析原因)。既然TB业务代码要使用一组block实现，我们就
需要明确怎么把一串IR划分为一组block。qemu IR要翻译成host指令，所以我们还要考虑IR
相关的翻译方法，这大概分为qemu自身控制相关的IR: insn_start/discard/exit_TB/goto_TB/
call等，以及具体业务相关的IR: 计算，控制流以及IO(load/store)。BlockISA中都要在
block里进行计算，要先把guest CPU寄存器的值load到T寄存器上，然后才能计算，我们需要
根据T寄存器的特点设计T寄存器分配规则。

我们考虑具体的实施办法以及测试方案。具体测试时，我们可以编译一个BlockISA的user
mode qemu，然后把这个qemu跑在一个当前模拟Block的qemu上，就是用两层qemu来做测试::

 ./qemu-linx(guest BlockISA, host X86) ./qemu-riscv(guest riscv, host BlockISA) app(riscv程序)

block划分
==========

一个TB翻译到后端BlockISA时block如何划分，一个TB一般由prologue、业务代码和epilogue
三部分组成，整个TB是一个host上的函数调用，prologue一般的工作是准备堆栈、保存callee
save寄存器、配置fix寄存器以及最后跳入业务代码执行，业务代码为实际翻译的host指令，
可以有跳转，epilogue为函数退出恢复环境，然后做函数返回。这样，我们可以把prologue/
epilogue分别组织到一个块里。为了简单，我们尝试把业务代码组织在一个块里，当业务代
码里有跳转时，我们用块内跳转支持，但是下面的分析马上会发现因为有call IR，导致业务
代码不可能完全在一个block里。

BlockISA定义中，当跳转的目的地址是块头时，只是使用块之间的跳转逻辑，也就是无法从
一个微指令直接跳到另外一个块的块头上。所以对于call IR，因为跳转的目的地址必然是
另外的一个块，就必须对业务代码分出block块用来做跳转。BlockISA的这个特点和传统ISA
是不同的。示意如下::

   +-----------------+           +------------------+
   |  IR0            |           |  IR0             |
   |  IR1            |           |  IR1             |
   |                 |           +------------------+
   |  label: IR2     |           |  label: IR2      |
   |  IR3            |           |  IR3             |
   |  IR4(call)      |           |  IR4(call)       |
   +-----------------+           +------------------+
   |  IR5            |           |  IR5             |
   |  IR6            |           |  IR6             |
   |  IR7(jump label)|           |  IR7(jump label) |
   +-----------------+           +------------------+

也就是IR4是一定要做block断开的。我们继续看还需要分块的场景，在没有call的TB内，如果
有分支IR，BlockISA可以生成块内跳转搞定，但是在有call的TB内，除了call会导致分块，
还有一种场景必须分块，就是被call分开的两个块之间存在分支IR。如上图所示，如果有IR7
向上跳到一个block块的中间，block是没法支持的，所以call+branch的IR最终导致在label
处还需要做block分块(右图)，其中以第一个块是fall，第二个是call，第三个是indcall。

因为，只有出现跨call的branch时，我们才不得不在label处分block，所以，实现上可以
在这样的条件出现时，我们才在label处分block(方案1)，也可以直接就在label处分block，
直接在label处分block，导致我们必须直接在branch处分block(方案2)。示意如下::

   +------------------+           +------------------+
   |  IR0             |           |  IR0             |
   |  IR1             |           |  IR1             |
   |  IR2             |           |  IR2             |
   |  IR3             |           |  IR3             |
   |  IR4(call)       |           |  IR4(call)       |
   +------------------+           +------------------+
   |  IR5             |           |  IR5             |
   |  IR6(jump label) |           |  IR6(jump label) |
   |                  |           +------------------+
   |  IR7             |           |  IR7             |
   |  IR8             |           |  IR8             |
   |                  |           +------------------+
   |  label: IR9      |           |  label: IR9      |
   |  IR10            |           |  IR10            |
   +------------------+           +------------------+

如上所示，对于没有跨块跳转的情况，本来可以按照左边分块(跳转用块内跳转搞定)，但是
如果使用统一的逻辑(方案2)，就需要按照右边分块。右边的分块基本上是qemu的BB的定义了
(除了call，qemu里call并不划分BB)。

方案1减少了分块，只在少数情况下才进一步拆分block，似乎性能会好，方案2基本依托qemu
的BB，似乎后面寄存器分配要好做一点。我们先选方案2。

我们考虑qemu加block head的具体方法::

  +------------------+
  |  IR0             |
  |  IR1             |
  |  IR2             | block 1
  |  IR3             |
  |  IR4(call)       |
  +------------------+
  |  IR5             | block 2
  |  IR6(jump label) |
  +------------------+
  |  IR7             | block 3
  |  IR8             |
  +------------------+
  |  label: IR9      | block 4
  |  IR10            |
  +------------------+

从信息量的角度上看，IR中已经包含了块划分的所有信息，所以，我们应该可以做到在后端
具体体系结构相关的代码中支持BlockISA head的识别和插入。但是，由于qemu中call/set_label/
br/brcond的代码很多在公共代码里处理，我们还要分析代码的细节才能做精确的判定。

对于块间跳转(set_label/br/brcond)的实现，本质上和传统ISA上指令间跳转的逻辑是一样，
传统ISA在跳转指令上，生成host指令的时候把label和跳转指令都先记录下来，随后使用label
的地址回填跳转指令的跳转目的地址，BlockISA不过是把要记录的跳转指令地址变成了块头
地址，把label的地址变成了label所对应的块头的地址。初步看来，这部分可以不改qemu公
共代码，可以在BlockISA后端实现中搞定。

call中间码的处理集中在qemu的公共代码里，我们需要细化一点去分析。在qemu看来call中
间码是一条自定义的指令，只不过除了输出寄存器，还会改变guest CPU的状态信息。所以
call中间码的输入输出寄存器和一般中间码的输入输出寄存器的生命周期处理的逻辑基本一样，
当call可能会改变guest CPU状态时，需要增加guest CPU的同步动作。

call的后端实现是构造一个host上的函数调用，具体要实现有::

 1. caller save寄存器保存和恢复。
 2. 函数入参寄存器分配以及参数更新。
 3. 参数过多时用栈传递函数入参。

对于caller save寄存器的保存和恢复，qemu检测caller save寄存器是否和虚拟寄存器存在
映射，如果存在就同步回内存，注意这里并不是保存到栈上。TB对应的这个host上的函数的
指令是动态生成的，目前并没有使用第一层的寄存器，所以这里应该并不需要做保存，不过
这里我们可以看见一个问题：BlockISA上内外两组寄存器的编码是重复的，在qemu上需要有
办法把他们区分开，直观想到的方法就是把内外两组寄存器的编号在qemu内部重新编排下，
在程序员可见的地方在映射回去。

普通ISA只要把函数入参同步到ABI定义的函数输入寄存器上就好，BlockISA要把入参同步到
第一层的函数输入寄存器上，这里具体实现上，可以在BlockISA后端代码中识别到是对函数
输入寄存器的处理，并生成set微指令把数据传到第一层寄存器。看起来无需改动qemu的公共
代码。使用栈传递函数参数和传统ISA的逻辑是一样的。

综合上述分析，目前看来不改qemu公共代码可以支持block的分块。不过可能要先尝试实现
下这部分代码，才能看的更清楚。

具体实现上，我们要在后端具体体系结构相关的代码中识别是否要插入head，并且维护一个
当前block的上下文，这个上下文用于收集block的信息，block结束用时收集到的信息回填
head的相关域段。我们分析在什么地方要插入head::

 1. TB一开始要插入head。
 2. set_label中间码之前要插入head。
 3. call中间码后要插入head。
 4. br/brcond中间码后要插入head。

如上1、2中，要在处理中间码之前插入块头，3、4中，要在处理中间码之后插入块头，这时
这个块头是给后面的中间码用的。3、4中间码的实现总会调用到host架构相关的代码里(tcg/linx)， 在tcg/linx里加入相关逻辑插入head，但是TB一开始插入head会比较难搞，因为每个中间码
都有可能是一个TB的第一条中间码，整个TB一开始的地方又没有host构架相关的回调函数接口，
一个可以想到的不改公共代码的方式是，因为只要生成host指令就一定要进入tcg/linx的具
体函数，我们就在每个指令生成函数里都判断一下是否是TB生成的第一条指令，如果是，就
加上head，以及调用block上下文的初始化函数，可见对于TB退出时，也要加上对应的信息
回填函数以及block上下文清理函数，一个TB可能会在exit_tb和goto_ptr退出，所以，可以
在解析到对应的中间码的时候做对应的回填和清空动作。set_label是qemu公共代码，这里
插入head也要改公共代码。

具体看，一个TB里block相关的上下文可以静态定义到host架构相关代码里。一个TB里的head
可以被顺序的挂入上下文里的head链表，head的信息可以在一个head解析完毕时回填，也可
以在TB解析结尾回填，跳转的信息在TB解析结尾回填。

看下call中间码具体的实现，从上面的经验中，我们可以看见只要是语义和qemu稍微变化一
点，实现上可能就会有问题，BlockISA和传统ISA一个显著的区别是BlockISA需要call需要
分块，而传统ISA上call甚至都不作为BB划分的依据，这样直接会导致call的虚拟寄存器活性
相关的语义会和qemu里对不上，BlockISA按call划分block，call的寄存器在call后都不用了
(当前block已经结束)，而qemu里认为call的虚拟寄存器还可能被后续使用，为此我们把call
也作为BB结束的条件，call的输入和输出寄存器都会dead，这个和BlockISA的语义就匹配了。
这里需要改qemu主流程。

实现call要支持如上三个功能，我们一个一个看下：

caller save寄存器保存和恢复。BlockISA是两层寄存器，caller save寄存器是第一层寄存
器，为了和qemu主流程里一层寄存器的抽象匹配，我们把第一层寄存器编码到和第二层寄存
器同一层，这样host代码里也知道要处理的是第一层寄存器还是第二层寄存器。qemu在检测
到一个寄存器是caller save的时候，就把它同步到内存里，因为我们统一分配的是第二层
寄存器，所以第一层寄存器并不会和虚拟寄存器绑定，所以这里的free是个空操作。

函数入参寄存器分配以及参数更新。我们会从第一层寄存器上分入参寄存器，qemu里分配寄
存器的流程一般是分配host物理寄存器并生产一个load指令把参数加载到分配的物理寄存器
上，但是对于BlockISA用于传递参数的寄存器是第一层寄存器，所以具体实现的时候，当host
load指令的作用对象是第一层的寄存器时，BlockISA的实现应该是先load到T寄存器上，然后
再生成一条set.g把参数传递到第一层寄存器上。注意，这里的语义和qemu的语义已经不同，
但是我们之前把call作为BB的结尾，call的输入输出寄存器已经head，不会再用，所以这里
不会有问题。

参数过多时用栈传递函数入参。这个合传统ISA的逻辑是一致的。

具体看下如上jump和call的head要怎么生成。qemu IR的jump会是直接跳转或者是条件跳转，
生成对应的head就好，v0.20支持的跳转最大范围是6+4，也就是正负1KB，我们认为支持顺序
排放的一个TB内的多个block足够了，如果不够就qemu减小TB大小，或者是生成concat块。
call IR如果用call normal head去实现，可能会超出跳转范围，为了简单起见，我们用
indirect call实现call。

目前，我们的设计是head和body依次顺序排放的，为了head 8 byte对齐，我们需要在body
结尾的时候跳过一些微指令再排放下一个head。v0.20的长立即数放到了body的结尾，所以，
总的看指令的排放是：head、body、长立即数、必要对齐空位、head...

寄存器分配
===========

考虑寄存器分配的具体做法。一个IR的输入寄存器要先放到host寄存器上才能参与计算，具
体到BlockISA上，要放到块内寄存器上(T寄存器和scratch寄存器)才能参与计算，而且这个
“放”的动作也需要微指令完成(v0.20可能在块头初始化时完成)，所以，对于IR的输入寄存器
可以依然插入load指令来做，只不过传统ISA是load到gpr上，BlockISA是load到T寄存器上。

一个IR的输出寄存器只能是T寄存器，但是T寄存器只有8个，本质上看输出寄存器分配光的
情况BlockISA和传统ISA是一样的，传统ISA怎么解决，BlockISA也可以用相应的办法解决，
就是把之前使用的寄存器换到内存上，这样就有空余的寄存器出来了。

继续考虑T寄存器独特的地方，思考具体怎么应对::

 1. 每个微指令不管有没有输出都要分一个T寄存器。
 2. 每过8个指令T寄存器会被强制更新(T寄存器的有效最大索引距离是8)。
 3. T寄存器按偏移索引。
 4. T寄存器只能顺序的申请和释放。

特点1，每个BlockISA微指令都要对应一个T寄存器。因为具体创建微指令都使用host架构自
定义的函数实现，我们只要针对没有T寄存器输出的微指令也顺序把翻译上下文里的T寄存器
标记为占用就好。

特点2和传统ISA明显不同，传统ISA寄存器按名字索引，寄存器的生命周期编译器可以完全
决定，而BlockISA的T寄存器受硬件的约束就比较大。按名字索引的，距离是多少都可能。
比如，得到微指令可能是::

  T0: const #5   
  T1: addi T0, #1
  T2: addi T0, #2
  T3: sub  T0, #3
  T4: addi T0, #1
  T5: addi T0, #2
  T6: sub  T0, #3
  T7: sub  T0, #3
  T0: sub  T0, #3  
  T1: sub  T0, #3   <---- 这条微指令已经看不到第一条微指令产生的T0。

所以，T寄存器更新要分情况来: 如果后面没有指令用旧T寄存器的值，我们可以直接用新指
令的输出覆盖，如果对应的T寄存器，后面还有指令要使用，就要插入一条微指令把T寄存器
值向下传递。每条有T寄存器需要覆盖的指令都要这样处理下::

  T0: const #5   
  T1: addi T0, #1
  T2: addi T0, #2
  T3: sub  T0, #3
  T4: addi T0, #1
  T5: addi T0, #2
  T6: sub  T0, #3
  T7: sub  T0, #3

  T0: copy from T0  <---- 这里T0没有dead，要插入一条COPY指令拷贝下T0的值。
  T1: sub  T0, #3   <---- T1已经dead，正常生成微指令。
  T2: sub  T0, #3   <---- 这里就可以索引到T0。

注意，判断T0有没有dead要想办法从本条指令开始向上找被覆盖的虚拟寄存器有没有dead，
这又和qemu的公共代码逻辑不一样，需要改公共代码。

特点3，T寄存器是按偏移索引的，这个也是传统的按名字索引的方式不一样。我们可以按名
字分配寄存器，生成微指令的时候再改成索引::

  T0: const #5                     T0: const #5
  T1: addi T0, #1     ---->        T1: addi offset_1, #1
  T2: addi T0, #2                  T2: addi offset_2, #2
  T3: sub  T0, #3                  T3: sub  offset_3, #3

这就需要生成每个block的微指令时，维护一个当前微指令是block中第几条指令的标记。这
个需要在qemu翻译上下文(TCGContext)里插代码。注意，如果block内部没有块内跳转，我们
可以这样做，但是，如果有块内跳转，offset需要按照实际执行顺序产生，想不出这里要怎
么搞?

特点4，T寄存器只能顺序的申请和释放。传统ISA的寄存器不用了就可以释放，qemu里的寄
存器分配代码的公共逻辑照此实现，但是BlockISA上不能这样，比如，已经使用了T0-T4，
到第五条微指令，也就产生T4的微指令发现T3 dead了，这个时候qemu不能释放T3，因为一
但释放，下次分配T寄存器就会分配T3，和我们的逻辑不符。注意，这个特性提醒我们T寄存
器可能有浪费，比如一个T寄存器可能就用了一次，那么要过8个微指令才能再次用它。

具体qemu的实现上，已经dead的寄存器不能释放掉，在要分T8时，应该刷新已经分配的T0-T7
的标记，把他们标记为没有分配，然后开始顺序分配T0-T7。这里要改qemu的公共代码，按照
基本语义讲，IR虚拟寄存器的生存周期和host物理寄存器分配特点是两个独立的概念，qemu
在公共代码里没有明确这两者的意义，qemu认为IR虚拟机寄存器dead，host物理寄存器就可以
释放，谁想还有BlockISA上这种T寄存器，但是现在的代码已经这样写，改起来真心头疼。

T寄存器顺序分配的特点还会和qemu中插入host指令的代码生成方式冲突。qemu在做具体寄
存器分配的时候，针对IR，是先分配IR的输入寄存器，再分IR的输出寄存器，然后再创建host
指令，创建指令之前qemu有可能再插入几条指令调整下后续指令的输入。我们拿riscv user
mode下的qemu_ld_i64中间码的模拟为例::

  tcg_out_opc_reg(s, OPC_ADD, base, TCG_GUEST_BASE_REG, addr_regl);
  tcg_out_qemu_ld_direct(s, data_regl, data_regh, base, opc, is_64);

如上代码在tcg/riscv/tcg-target.c.inc。这里是指令生成的代码，而在这之前，qemu的
公共代码里已经完成了输入输出寄存器分配，如上的代码是在生成ld指令之前通过guest PA
计算host VA，最后的load指令需要操作host VA。对于传统ISA，qemu的实现是reserve出几
个temp寄存器，专门处理这种情况::

  add_i64 Rd, Rs, imm              add_i64 Rd, Rs, imm
                                   add_i64 reserv_tmp, Rd, TCG_GUEST_BASE_REG
  ld_i64 R_data, Rd                ld_i64 R_data, reserv_tmp

但是，BlockISA的输出都要通过T寄存器，所以针对这种情况，BlockISA必须把之前已经分
配出的输出寄存器(T1)，重新分配给新插入的微指令，并且重新为下面的指令分配输出寄存
器(T2)::

  T0: add_i64 Rs, imm              T0: add_i64 Rs, imm
                                   T1: add_i64 offset_1, TCG_GUEST_BASE_REG
  T1: ld_i64 offset_1              T2: ld_i64 offset_1

这会带来严重问题，改动一条指令的输入寄存器只要保证本条指令的逻辑正确即可，但是改
动一条指令的输出还要处理后续指令的依赖问题。不过好在qemu中host指令是一个一个生成
的，简单的办法是重新调整下ld_i64的输出寄存器，重新把T1改成T2。

如果出现后面的寄存器已经分配，但是需要插入微指令的场景(目前没有看到这样的场景)，
T寄存器的相对寻址会都乱掉。一个笨办法是在插入指令之前把T0-T7的值保存起来，插入完
指令后再恢复T0-T7的值，中间就可以随意插入微指令计算ld_i64的输入，然后把输入放到一
个新的scratch寄存器上，修改ld_i64的输入寄存器为新的scratch寄存器，注意这里不能修
改T寄存器的值，因为T寄存器除了被ld_i64使用，还有可能被后面的指令所使用，在v0.20
的版本中，已经有ld指令可以用scratch寄存器作为输入，所以这样搞逻辑上应该没有问题。
如下是一个简单示意::

  T0: add_i64 Rs, imm    T0: add_i64 Rs, imm

                             copy offset8 to scratch1
                             copy offset8 to scratch2
                             ...
                             copy offset8 to scratch8  -> T0-T7保存起来了

                         T1: add_i64 scratch8, TCG_GUEST_BASE_REG -> 使用T0-T7
                             sset scratch9, offset_1   -> 计算结果保存到新scratch
                                                          寄存器

                             copy from scratch1
                             copy from scratch2
                             ...
                             copy from scratch8        -> 恢复T0-T7，

  T1: ld_i64 offset_1    T1: ld_i64 scratch9           -> 更新输入寄存器

Fixed cpu_env实现
==================

Fixed实现的cpu_env的指针，在BlockISA上是不能继续放到gpr上的，因为BlockISA要在
block里完成计算，所以我们可以直接把cpu_env绑定到block内的一个scratch寄存器上，可
以先定成scratch0。这样，每一个block都要fixed寄存器的值传入，这样又要占用一个gpr，
原来的fixed实现语义硬生生的被搞成了非fixed，真是悲催。

如果存在block内可以直接索引的全局寄存器，这个问题就可以解决，但是这又和使用block
传递信息的设计相违背，BlockISA可以拆出一些gpr，这些gpr不用来做block之间的参数传递，
这样block内部就可以直接引用，我们用这样的gpr做fixed类型的寄存器就好。

具体IR实现上的考虑
===================

1. goto_tb和exit_tb的实现

考虑goto_tb和exit_tb的实现，这两个IR会创建BB出来，也就是有独立的block与之对应，
对于exit_tb，它对应的block固定的跳转到epilogue块，根据exit_tb的参数刷新gpr就好，
对于goto_tb，它对应的block首先应该是fall，然后在翻译上下文里改变block的跳转类型
和跳转地址。我们把goto_tb作为高级特性，相关详细设计延后。

2. gen_tb_start的实现

qemu在开始翻译业务代码的时候，在gen_tb_start里插入了几条中间码，这个并不破坏我们
之前的分析，无非是之前认为没有call/goto_tb/br/brcong和set_label，所有业务代码可以
放在一个TB里，现在加了gen_tb_start里的中间码，因为里面会有brcond，我们最简的实现
也要至少分两个block了。不过我们最简的实现也可以先把gen_tb_start这个函数以及对应
set_label的代码删去，先聚焦寄存器分配吧。

具体实现步骤上的考虑
=====================

先不考虑复杂情况，我们先实现一个有TB头有TB尾，可以分配寄存器可以跑起来的qemu，先
不管call，goto_tb，br/brcong和set_label等等。这样TB的业务代码我们可以先不考虑block
划分的实现，我们先专心搞寄存器分配。这一步的目标可以是实现一个riscv add指令在
BlockISA上的模拟。

等到有一个hello world的版本后，我们依此实现call，goto_tb，br/brcong和set_label等
特性。这一步完成，可以看做BlockISA qemu后端涉及的基本技术都已经打通。

基于以上，实现所有qemu的IR。到这里可以认为我们有了一个可以运行的BlockISA qemu
后端。

把块内跳转和scratch寄存器引入到BlockISA qemu后端实现中，这一步的目的是提高模拟
性能。

注意::

 目前看v0.13的ISA设计已经太老了，我们有必要基于v0.20 ISA做设计，但是目前我们只在
 v0.13的编译器上成功的编译了BlockISA qemu，所以，我们先基于v0.20做完全部设计，写
 必要的demo代码，然后再去折腾具体实现吧。

块内跳转和scratch寄存器的考虑
==============================

目前，我们没有考虑生成块内跳转指令，也没有考虑把scratch寄存器纳入寄存器分配，目前
看scratch寄存器按照名字索引，可以克服T寄存器带来的不便，我们尝试把scratch寄存器
拉进来一起考虑下，在v0.20上，scratch寄存器被整合到local gpr，我们后面都叫lgpr。

我们先考虑一种最粗暴的方式，那就是寄存器尽可能全部用lgpr，但是v0.20中必须用T寄存器
做输出寄存器，所以还是一定要用T寄存器作为IR的输出寄存器，但是我们立马生成一条set
指令把T寄存器的输出写到一个lgpr上，这样对于一条IR的输出，我们可以给它分三个host物
理寄存器：两个顺序的T寄存器和一个lgpr，比如，对于一条add_i64 t0, t1, t2，对于t0
我们可以分配T0/T1/RSL2，对应翻译的host指令可以是::

 T0: add RSL0, RSL1
 T1: set t#1, RSL2

如上，add的输出先到T0，再被传递到RSL2，T1是必要的占位T寄存器。在qemu的中间码看来，
虚拟寄存器只被映射到lgpr，就是RSL2，这样T寄存器虽然依然存在，但基本上已经和qemu
主流程无关，反而T寄存器可以作为临时寄存器在必要的时候使用。不过可以预见，这样实现
产生的指令会膨胀。

T寄存器作为临时寄存器使用时，可以不必要提前reserve出来，我们再看下之前要插入指令
的情况::

  add_i64 Rd, Rs, imm              add_i64 Rd, Rs, imm
                                   add_i64 reserv_tmp, Rd, TCG_GUEST_BASE_REG
  ld_i64 R_data, Rd                ld_i64 R_data, reserv_tmp

传统ISA上是这样做的，reserv_tmp并不参与普通的寄存器分配，在BlockISA上可以这样::

  add_i64 Rd, Rs, imm              add_i64 Rd, Rs, imm

                                   if 不需要插入指令修改ld地址:
                                       T0: sget Rd
                                   else:
                                       T1: add Rd, TCG_GUEST_BASE_REG
  ld_i64 R_data, Rd                ld_i64 R_data, t#1, 0

因为我们的前提是已经都用lgpr作为指令的输入和指令的最终输出，T寄存器反而可以充当
reserv_tmp的角色，这样我们都不用像传统ISA那样reverve出寄存器专门处理这样的情况了。

v0.20上，lgpr被整合到local gpr上，这样会有两个分配者，一个是作为块输入的lgpr，一
个是作为scratch寄存器的lgpr，不过似乎除了call，估计也不会用到gpr和local gpr，我们
需要具体分析call的实现(todo)。

块内跳转可以和lgpr一起使用，解决块内跳转引入的T寄存器无法动态索引的问题。我们可以
在br/brcond IR上引入块内跳转，这样，最终的情形是，只是在BB内使用T寄存器(对应normal
temp寄存器)，当br/brcond导致跨BB，我们使用lgpr保存local temp。这样的设计可能会导
致local_gpr被闲置，因为local temp使用的并不多，还可能导致频繁的出现T寄存器的copy，
为此，我们可以分一些lgpr给normal temp使用。

阶段总结
=========

1. BlockISA虽然和传统ISA很不一样，但是我们在块内以lgpr作为host物理寄存器分配主体，
   T寄存器作为补充，基本可以做到不怎么修改qemu主逻辑。

   按照如上分析的一个demo的代码在：
   https://codehub-y.huawei.com/LinxISAV150/LinxBlockModel/files?ref=qemu_backend_dev

2. 还有如下特性需要设计::

 1. 考虑把head和body分开存放
 2. concat支持
 3. goto tb
 4. 加入块内跳转指令
 5. 脱离qemu的限制，考虑有没有适合BlockISA的后端适配方法
